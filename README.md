# terraform-export-google-spanner

The terraform example of exporting from Cloud Spanner database to Avro in GCS bucket.

Input variables:
- project_id
- region
- bucket_name
- spanner_instance
- spanner_db

See `example/variables.tf` for more detailes.
